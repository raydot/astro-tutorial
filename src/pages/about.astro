---
import '../styles/global.css'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
const pageTitle="About Me";

const identity = {
    firstName: "Larry",
    country: "Djbouti",
    occupation: "Haberdasher",
    hobbies: ["finery", "winery", "tinery"]
}

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Haberdashery"]

const rando1 = Math.floor(Math.random() * identity.hobbies.length)
const rando2 = Math.floor(Math.random() * identity.hobbies.length)

const happy = true;
const finished = false;
const goal = 3;

const skillColor="navy"

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{pageTitle}</title>
        <style define:vars={{skillColor}}>
            h1 {
                color: green;
                font-size: 4rem;
            }
            .skill {
                color: red;
                color: var(--skillColor);
                font-weight: bold;
            }
        </style>
	</head>
	<body>
        <Header />
		<h1>Astro the Magnificent</h1>
		<h1>{pageTitle}</h1>
        <h2>...and my new Astro site!</h2>

        <p>Astro has given me some text that they want me to use here, but I can't be bothered.</p>

        <p>If you'd like to see it for yourself, <a href="https://docs.astro.build/en/tutorial/2-pages/1/"></a>read the tutorial</a>.</p>

        <p>Here are a few (non) facts about me:</p>
        <ul>
            <li>My name is {identity.firstName}</li>
            <li>I live in {identity.country} and I work as a {identity.occupation}</li>
            {identity.hobbies.length >= 2 &&
                <li>Two of my hobbies are: {identity.hobbies[rando1]} and also {identity.hobbies[rando2]}</li>

            }
        </ul>
        <p>My skills are:</p>
        <ul>
            {skills.map((skill) => <li class="skill">{skill}</li>)}
        </ul>

        {happy && <p>I am happy today!</p>}
        {finished && <p>I have finished the Astro tutorial!</p>}
        {goal === 3 ? <p>My goal is to finish in {goal} days</p> : <p>Some other condition using the number {goal}</p> }

        <Footer />  
        <script>
			import "../scripts/menu.js"
		</script>
	</body>
</html>